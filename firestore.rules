rules_version = '2';
// DEV rules: any signed-in user can read/write. Tighten by role before production
// (spec §20: API must check role permissions).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
