{
  "name": "bizgo-noti-server",
  "version": "1.0.0",
  "description": "BizGo push notification worker — bridges Firestore `notifications` to FCM push, routed by user role.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test-send": "node src/testSend.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^13.0.1"
  }
}
