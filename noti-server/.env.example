# Đường dẫn tới file service account (JSON) của Firebase Admin SDK.
# Mặc định trỏ tới file đã có ở thư mục cha (d:\App_BizGo\).
SERVICE_ACCOUNT_PATH=../bizgo-877df-firebase-adminsdk-fbsvc-e6eb395320.json

# Chỉ gửi push cho notification tạo SAU khi server khởi động (true) hay xử lý cả
# các doc chưa gửi có sẵn (false). Mặc định true để tránh spam lịch sử khi chạy lần đầu.
ONLY_NEW=true
